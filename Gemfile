source "http://rubygems.org"

# not needed if using thin
# gem "eventmachine"
gem 'thin'
gem "rack-async"

# eventmachine postgresql gem, async DB calls
# Blocks getting SQL results!!!
#gem "em-pg-client", "~> 0.2.0", :require => 'pg/em'

gem 'pg'
gem "em-postgres"
gem 'json'

gem 'ruby-debug19', :group => :development
